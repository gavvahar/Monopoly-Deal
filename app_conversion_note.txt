# Flask app moved to app_flask_backup.py
# The application has been converted to FastAPI in main.py

# To run the new FastAPI application:
# uvicorn main:app --host 0.0.0.0 --port 8000

# Or use the Docker setup which has been updated to use FastAPI